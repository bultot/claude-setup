# =============================================================================
# CC Remote Workspace — SSH config for MacBook
# =============================================================================
# Append this to ~/.ssh/config (or run scripts/setup-macbook.sh)
#
# Usage:
#   ssh cc              → drops into default Zellij session on VPS
#   ssh cc-raw          → plain SSH (no Zellij, for scp/rsync/scripting)
# =============================================================================

# --- Claude Code VPS (via Tailscale) ----------------------------------------

Host cc
    HostName claude-code
    User robin
    RequestTTY yes
    RemoteCommand zellij attach --create main
    # Keep connection alive over flaky networks / lid-close
    ServerAliveInterval 30
    ServerAliveCountMax 3

Host cc-raw
    HostName claude-code
    User robin

# --- End CC Remote Workspace ------------------------------------------------
